import {MainVisual} from './MainVisual.jsx';

window.addEventListener('DOMContentLoaded',function(){
	//const MainVisualEl=document.getElementById('MainVisual');
	//ReactDOM.render(<MainVisual slides={Array.from(MainVisualEl.children)}/>,MainVisualEl);
});